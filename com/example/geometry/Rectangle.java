package com.example.geometry;

public class Rectangle {
    public void area(double length, double breadth){
        length = length * breadth;
        System.out.println("Area of Rectangle is: " + length);
    }
}
